name: Run gradle build
on: [push]
jobs:
  build_job:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - uses: DeLaGuardo/setup-graalvm@5.0
      with:
        graalvm: '21.0.0.2'
        java: 'java11'
        arch: 'amd64'
    - uses: actions/setup-java@v2
      with:
        java-version: 11
        distribution: temurin
        cache: 'gradle'
    - uses: gradle/gradle-build-action@v2
      env:
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
#        arguments: clean test distZip sonarqube --info --stacktrace
        arguments: clean distZip nativeCompile --info --stacktrace
    - name: Release
      uses: softprops/action-gh-release@v1
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: |
          cli/build/distributions/mycliapp.zip
          cli/build/native/nativeCompile/mynativeapp
